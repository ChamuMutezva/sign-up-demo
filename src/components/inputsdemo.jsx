import React from 'react'

function inputsdemo() {
    return (
        <div>
            {/*
         <div className='customer-email form-input-container'>
              <label htmlFor="email">Email</label>
              <input type="email"
                name="email"
                id="email"
                placeholder="alexei@mail.com"
                autoComplete="email"
                onChange={onChange}
                value={email}
                min={5}
                max={50}
                required
                onBlur={onBlur}
                className="form-input border-radius"
                aria-describedby="error-email"
              />

              {errors.email && <small id="error-email"
                aria-live="polite"
                className="error__alert">{errors.email}</small>}

            </div>

            
            <div className='customer-name form-input-container'>
              <label htmlFor="firstname">First Name</label>
              <input type="text"
                name="firstName"
                id="firstname"
                placeholder="Alex Ward"
                autoComplete='name'
                onChange={onChange}
                value={firstName}
                min={1}
                max={50}
                required
                aria-required={true}
                onBlur={onBlur}
                className='form-input  border-radius'
                aria-describedby="error-first-name"
              />

              {errors.firstName && <small className={`error__alert`}
                aria-live="polite"
                id="error-first-name">{errors.firstName}</small>}

            </div>

           
            <div className='customer-lastname form-input-container'>

<label htmlFor="lastname">Last Name</label>
<input type="text"
  name="lastName"
  id="lastname"
  placeholder="Ward"
  autoComplete='name'
  onChange={onChange}
  value={lastName}
  min={1}
  max={50}
  required
  aria-required={true}
  onBlur={onBlur}
  className='form-input  border-radius'
  aria-describedby="error-lastname"
/>

{errors.lastName && <small className={`error__alert`}
  aria-live="polite"
  id="error-lastname">{errors.lastName}</small>}

</div>

 
            <div className='customer-password form-input-container'>

<label htmlFor="password">password</label>
<input type="password"
  name="password"
  id="password"
  placeholder="*****"
  onChange={onChange}
  value={password}
  min={4}
  max={6}
  required
  aria-required={true}
  onBlur={onBlur}
  className='form-input  border-radius'
  aria-describedby="error-password"
/>

{errors.password && <small className={`error__alert`}
  aria-live="polite"
  id="error-password">{errors.password}</small>}

</div>
           

  */}
        </div>
    )
}

export default inputsdemo